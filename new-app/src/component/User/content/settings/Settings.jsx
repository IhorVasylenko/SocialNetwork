import React from "react";
import s from './Settings.module.css';

const UserContentSettings = (props) => {
    return (
        <div className={s.wrapper}>body</div>
    )
}

export default UserContentSettings